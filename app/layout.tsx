import type { Metadata } from 'next';
import localFont from 'next/font/local';
import {
  NavigationMenu,
  NavigationMenuList,
  NavigationMenuLink ,
} from '@/components/ui/navigation-menu';
import './globals.css';

const geistSans = localFont({
  src: './fonts/GeistVF.woff',
  variable: '--font-geist-sans',
  weight: '100 900',
});
const geistMono = localFont({
  src: './fonts/GeistMonoVF.woff',
  variable: '--font-geist-mono',
  weight: '100 900',
});

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang='en'>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <NavigationMenu className='bg-white fixed top-0 w-full text-lg font-medium'>
          <NavigationMenuList className='flex gap-4 p-8'>
            <NavigationMenuLink href='#home'>Home</NavigationMenuLink>
            <NavigationMenuLink href='#aboutme'>About Me</NavigationMenuLink>
            <NavigationMenuLink href='#contactme'>Contact</NavigationMenuLink>
          </NavigationMenuList>
        </NavigationMenu>
        {children}
      </body>
    </html>
  );
}
