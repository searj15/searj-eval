/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com', 'images.credly.com', 'pluspng.com'],
    },
  };
  
  export default nextConfig;
  